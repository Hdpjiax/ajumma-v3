// Componente desactivado — se reemplazara por otra funcion
export default function PushBanner() { return null }
export function usePushNotifications() { return { permission: "denied", subscription: null, register: () => null } }
